name: Automated Data Generation

on:
  schedule:
    # Run every 2 hours
    - cron: '0 */2 * * *'
  
  workflow_dispatch:  # Allow manual trigger
  
  push:
    branches: [ main, master ]
    paths:
      - '**.py'
      - '.github/workflows/**'

jobs:
  generate-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for proper commits
    
    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    
    - name: 🗄️ Check database exists
      run: |
        if [ ! -f retail_pos.db ]; then
          echo "Database not found, will be created on first run"
        else
          echo "✓ Database found"
          ls -lh retail_pos.db
        fi
    
    - name: 📊 Generate Categories
      run: python 01_generate_categories.py
      continue-on-error: true
    
    - name: 🏢 Generate Suppliers
      run: python 02_generate_suppliers.py
      continue-on-error: true
    
    - name: 👥 Generate Staff
      run: python 03_generate_staff.py
      continue-on-error: true
    
    - name: 🖥️ Generate Machines
      run: python 04_generate_machines.py
      continue-on-error: true
    
    - name: 💳 Generate Payment Methods
      run: python 05_generate_payment_methods.py
      continue-on-error: true
    
    - name: 📋 Generate Transaction Types
      run: python 06_generate_transaction_types.py
      continue-on-error: true
    
    - name: 🏷️ Generate Product Groups
      run: python 07_generate_product_groups.py
      continue-on-error: true
    
    - name: 📦 Generate Products
      run: python 08_generate_products.py
      continue-on-error: true
    
    - name: 🧾 Generate Transaction Headers
      run: python 09_generate_transaction_headers.py
      continue-on-error: true
    
    - name: 📝 Generate Transaction Lines
      run: python 10_generate_transaction_lines.py
      continue-on-error: true
    
    - name: 📈 Generate Statistics Report
      run: python generate_stats_badge.py
      continue-on-error: true
    
    - name: 📤 Commit and Push Changes
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add .
        git diff --staged --quiet || git commit -m "🤖 Automated data generation $(date +'%Y-%m-%d %H:%M:%S UTC')
        
        Generated by GitHub Actions
        Workflow: ${{ github.workflow }}
        Run: ${{ github.run_number }}"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 📊 Upload Database Artifact
      uses: actions/upload-artifact@v4
      with:
        name: retail-pos-database
        path: retail_pos.db
        retention-days: 30
    
    - name: 📋 Upload Logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: generation-logs
        path: logs/*.log
        retention-days: 7